[Unit]
RequiresMountsFor=/opt/observability-data
ConditionPathIsMountPoint=/opt/observability-data

[Service]
# Refuse to put persistent telemetry on the OS disk when the data mount is absent.
ExecStartPre=/usr/bin/mountpoint --quiet /opt/observability-data
